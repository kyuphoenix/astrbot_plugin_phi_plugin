{
  "default_global": {
    "description": "默认使用国际服存档接口",
    "type": "bool",
    "default": false
  },
  "render_mode": {
    "description": "渲染模式；image 使用 AstrBot HTML/Playwright 图片面板，text 返回纯文本",
    "type": "string",
    "default": "image",
    "options": ["text", "image"]
  },
  "render_backend": {
    "description": "图片渲染后端；当前固定使用 AstrBot HTML/Playwright 渲染",
    "type": "string",
    "default": "html",
    "options": ["html"]
  },
  "score_image_version": {
    "description": "Single-score image template version; modern uses score/score, old uses score/scoreOld",
    "type": "string",
    "default": "modern",
    "options": ["modern", "old"]
  },
  "ranklist_image_version": {
    "description": "Ranklist image template version; modern uses rankingList/rankingList, old uses rankingList-old/rankingList",
    "type": "string",
    "default": "modern",
    "options": ["modern", "old"]
  },
  "max_b30": {
    "description": "B30 展示条数",
    "type": "int",
    "default": 33,
    "slider": {"min": 33, "max": 50, "step": 1}
  },
  "list_score_max_num": {
    "description": "list score filter max displayed rows; larger results ask users to narrow filters",
    "type": "int",
    "default": 80,
    "slider": {"min": 1, "max": 500, "step": 1}
  },
  "history_day_num": {
    "description": "phi update 每个日期最多展示的历史成绩数量，至少为 2，默认与原版一致为 10。",
    "type": "int",
    "default": 10,
    "slider": {"min": 2, "max": 50, "step": 1}
  },
  "history_score_date": {
    "description": "phi update 最多展示的历史日期数量，默认与原版一致为 10。",
    "type": "int",
    "default": 10,
    "slider": {"min": 1, "max": 60, "step": 1}
  },
  "history_score_num": {
    "description": "phi update 最多展示的历史成绩总数，默认与原版一致为 50。",
    "type": "int",
    "default": 50,
    "slider": {"min": 1, "max": 300, "step": 1}
  },
  "api_base_url": {
    "description": "Phi Plugin 联合查分 API 地址",
    "type": "string",
    "default": "https://phib19.top:8080"
  },
  "request_timeout": {
    "description": "网络请求超时时间（秒）",
    "type": "int",
    "default": 10,
    "slider": {"min": 3, "max": 60, "step": 1}
  },
  "qrcode_timeout": {
    "description": "TapTap 二维码登录等待时间（秒）",
    "type": "int",
    "default": 270,
    "slider": {"min": 30, "max": 600, "step": 10}
  },
  "render_max_retries": {
    "description": "HTML/T2I 渲染失败后的最大重试次数",
    "type": "int",
    "default": 2,
    "slider": {"min": 0, "max": 5, "step": 1}
  },
  "github_proxy": {
    "description": "GitHub 代理地址，当前仅预留",
    "type": "string",
    "default": ""
  },
  "illustration_source": {
    "description": "Illustration source: remote passes GitHub raw URLs from Catrong/phi-plugin-ill into Jinja2 templates; local encodes downloaded files as base64 for T2I",
    "type": "string",
    "default": "remote",
    "options": ["local", "remote"]
  },
  "illustration_url_proxy": {
    "description": "Remote illustration URL proxy for T2I rendering. Example: https://ghproxy.net . Empty means direct GitHub raw URLs.",
    "type": "string",
    "default": ""
  }
}
